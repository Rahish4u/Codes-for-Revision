// let's start understanding about Object

let product = {
    name : "BoatRockerz",
    company : "Boat",
    price : "2000",
    warranty : "2 years",
    color : "Light Blue"
}
console.log(product);

// object method
/*
1. Object.keys() -- when we need fetch all the keys we use it.
2. Object.values() -- if we want to fetch all available values we can use.
3. Object.entries() -- instead of fetching keys and values seprately, we can use it to fetch complete pair.
4. Object.keys().length -- no of keys value pair .
*/


